// Importando el módulo operaciones
const operaciones = require('./operaciones')

console.log(operaciones);

// require no sólo puede importar módulos sino que tambien puede importar archivos json

const users = require('./users.json')

console.log(users);